{
  "name": "znhip-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web && npm run build --prefix api",
    "install": "npm install --prefix web && npm install --prefix api"
  }
}
